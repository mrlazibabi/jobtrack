# schema/

Hand-written DDL for the practice database `JobTrackPractice` (weeks 4–7). Reference only: the application schema is owned by EF Core migrations in `src/`.

Suggested files as the schema evolves:

- `v1-week4.sql` — first attempt (Companies, JobApplications only)
- `v2-week5.sql` — with constraints, Interviews, Notes
- `v3-week7.sql` — after normalization review, matching `docs/erd.md`
